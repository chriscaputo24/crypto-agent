import React from "react";
import ReactDOM from "react-dom/client";
import CryptoAgent from "./CryptoAgent.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CryptoAgent />
  </React.StrictMode>
);